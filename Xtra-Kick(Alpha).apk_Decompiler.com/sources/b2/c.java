package b2;

import android.view.Surface;
import s1.n;
import s1.q;

public final class c extends n {
    public c(IllegalStateException illegalStateException, q qVar, Surface surface) {
        super(illegalStateException, qVar);
        System.identityHashCode(surface);
        if (surface != null) {
            surface.isValid();
        }
    }
}
